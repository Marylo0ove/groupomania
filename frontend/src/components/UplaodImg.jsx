import React from 'react';

/*
const UplaodImg = () => {
    const [file, setFile] = useState();
const dispatch = useDispatch();
const userData = useSelector((state) => stateUserReducer);

    const handlePicture = () => {

    }
    return (
        <div>
       <form action="" onSubmit={handlePicture} className="upload-pic">
       <label htmlFor="file">Changer mon image de rofil</label>
       <input type="file" id='file' name='file' accept='.jpg .jpeg .png' onChange={(e) => setFile(e.target.files[0])}/>
       </form>
       <input type="submit" value="Envoyer" />x
       </div>
    );
};

export default UplaodImg;

*/

